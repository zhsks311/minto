import SwiftUI

/// Minto 브랜드 색상 토큰.
/// `docs/ui-design-guide.md`의 대표 색상을 코드에서 재사용하기 위한 최소 토큰이다.
public enum MintoDesignTokens {
    public static let brandMintLight = Color(red: 236.0 / 255.0, green: 248.0 / 255.0, blue: 242.0 / 255.0)
    public static let brandMint = Color(red: 131.0 / 255.0, green: 226.0 / 255.0, blue: 193.0 / 255.0)
    public static let brandMintDeep = Color(red: 115.0 / 255.0, green: 184.0 / 255.0, blue: 158.0 / 255.0)
    public static let brandTeal = Color(red: 1.0 / 255.0, green: 82.0 / 255.0, blue: 84.0 / 255.0)
}
